# Meta_Kim 安装写入清单

本文档整理本次在当前仓库执行 Meta_Kim 安装与验证流程后，实际写入到 `Claude Code` 与 `Codex` 的配置内容。

为避免乱码，本文档内容使用中文编写，并按 UTF-8 编码保存。

## 一、总览

本次安装写入分为三类：

1. 项目内运行时投影
2. 用户目录下的全局配置
3. 安装状态记录

其中：

- `Claude Code` 同时写入了项目内配置和用户级全局配置
- `Codex` 明确写入了项目内配置，并安装了用户级全局技能包

## 二、统一写入清单

| 运行时 | 范围 | 位置 | 作用 |
| --- | --- | --- | --- |
| Claude Code | 项目内 | `.claude/agents/` | 写入 9 个 Meta agents，供当前仓库内的 Claude 使用 |
| Claude Code | 项目内 | `.claude/skills/meta-theory/` | 写入项目级 `meta-theory` 技能包及参考资料 |
| Claude Code | 项目内 | `.claude/hooks/` | 写入项目级 hook 脚本集合 |
| Claude Code | 项目内 | `.claude/settings.json` | 写入当前仓库专用的 Claude 权限与 hook 配置 |
| Claude Code | 项目内 | `.mcp.json` | 为当前仓库注册 `meta-kim-runtime` MCP 服务 |
| Claude Code | 全局 | `C:/Users/zhouy/.claude/skills/meta-theory/` | 安装全局 `meta-theory` 技能，影响这台机器上的 Claude |
| Claude Code | 全局 | `C:/Users/zhouy/.claude/hooks/meta-kim/` | 安装全局 hook 脚本目录 |
| Claude Code | 全局 | `C:/Users/zhouy/.claude/settings.json` | 将 Meta_Kim 的 hook 配置合并到用户级 Claude 设置 |
| Codex | 项目内 | `.codex/agents/` | 写入 9 个 Meta agents，供当前仓库内的 Codex 使用 |
| Codex | 项目内 | `.codex/skills/meta-theory/` | 写入项目级 `meta-theory` 技能包及参考资料 |
| Codex | 项目内 | `.codex/hooks/` | 写入项目级 hook 脚本集合 |
| Codex | 项目内 | `.codex/hooks.json` | 写入当前仓库专用的 Codex hook 配置 |
| Codex | 项目内 | `.codex/commands/meta-theory.md` | 写入项目级 `meta-theory` 命令入口 |
| Codex | 全局 | `C:/Users/zhouy/.codex/skills/meta-theory/` | 安装全局 `meta-theory` 技能，影响这台机器上的 Codex |

## 三、Claude Code 实际写入内容

### 1. 项目内写入

当前仓库内写入了以下 Claude 相关配置：

- `.claude/agents/`
- `.claude/skills/meta-theory/`
- `.claude/hooks/`
- `.claude/settings.json`
- `.mcp.json`

这些内容只对当前仓库生效。

### 2. 项目级 `.claude/settings.json` 的实际作用

该文件中实际启用了以下行为：

- 权限拒绝规则
  - 禁止读取 `.env`
  - 禁止读取 `.env.*`
  - 禁止读取 `secrets/**`
  - 禁止读取 `*.pem`
  - 禁止读取 `*.key`
- `PreToolUse`
  - 在执行 `Bash` 前插入 graphify 提示
  - 调用 `block-dangerous-bash.mjs`
  - 调用 `pre-git-push-confirm.mjs`
  - 对 `Write|Edit|Bash|Agent|MultiEdit|NotebookEdit` 调用 `enforce-agent-dispatch.mjs`
  - 在 `Skill` 事件上调用 `activate-meta-theory-spine.mjs`
- `PostToolUse`
  - 在 `Edit|Write` 后调用 `post-format.mjs`
  - 在 `Edit|Write` 后调用 `post-typecheck.mjs`
  - 在 `Edit|Write` 后调用 `post-console-log-warn.mjs`
- `SubagentStart`
  - 调用 `subagent-context.mjs`
- `Stop`
  - 调用 `stop-memory-save.mjs`
  - 调用 `stop-compaction.mjs`
  - 调用 `stop-console-log-audit.mjs`
  - 调用 `stop-completion-guard.mjs`
  - 调用 `stop-spine-cleanup.mjs`

### 3. 项目级 `.mcp.json` 的实际作用

该文件为当前仓库注册了一个 MCP 服务：

- 服务名：`meta-kim-runtime`
- 启动方式：`node`
- 启动脚本：`D:/knowledgeBase/Meta_Kim/scripts/mcp/meta-runtime-server.mjs`

### 4. 用户级全局写入

本次安装在用户目录下为 Claude 写入了以下内容：

- `C:/Users/zhouy/.claude/skills/meta-theory/`
- `C:/Users/zhouy/.claude/hooks/meta-kim/`
- `C:/Users/zhouy/.claude/settings.json`

其中全局 hook 目录包含这些脚本：

- `block-dangerous-bash.mjs`
- `enforce-agent-dispatch.mjs`
- `post-console-log-warn.mjs`
- `post-format.mjs`
- `post-typecheck.mjs`
- `pre-git-push-confirm.mjs`
- `spine-state.mjs`
- `stop-compaction.mjs`
- `stop-completion-guard.mjs`
- `stop-console-log-audit.mjs`
- `stop-memory-save.mjs`
- `stop-save-progress.mjs`
- `stop-spine-cleanup.mjs`
- `subagent-context.mjs`
- `utils.mjs`

### 5. 用户级 `C:/Users/zhouy/.claude/settings.json` 中可确认的合并结果

当前可确认 Meta_Kim 已将以下 hook 规则合并到用户级 Claude 设置：

- `PreToolUse`
  - `block-dangerous-bash`
  - `pre-git-push-confirm`
- `PostToolUse`
  - `post-format`
  - `post-typecheck`
  - `post-console-log-warn`
- `SubagentStart`
  - `subagent-context`
- `Stop`
  - `stop-compaction`
  - `stop-console-log-audit`
  - `stop-completion-guard`
  - `stop-memory-save`
  - `stop-save-progress`

说明：

- 用户级 `settings.json` 中原本还存在其他环境变量和配置项
- 本文档只记录本次可明确归因于 Meta_Kim 安装的写入或合并结果
- 不重复记录敏感值

## 四、Codex 实际写入内容

### 1. 项目内写入

当前仓库内写入了以下 Codex 相关配置：

- `.codex/agents/`
- `.codex/skills/meta-theory/`
- `.codex/hooks/`
- `.codex/hooks.json`
- `.codex/commands/meta-theory.md`

这些内容只对当前仓库生效。

### 2. 项目级 `.codex/hooks.json` 的实际作用

该文件中实际启用了以下行为：

- `SessionStart`
  - 在 `startup|resume` 时执行 `meta-kim-memory-save.mjs --event session-start`
- `UserPromptSubmit`
  - 执行 `meta-kim-memory-save.mjs --event user-prompt`
  - 执行 `hookprompt-adapter.mjs`
- `PreToolUse`
  - 在 `Bash` 前执行 `graphify-context.mjs`
- `Skill`
  - 在触发 `meta-theory` 技能时执行 `activate-meta-theory-spine.mjs`
- `Stop`
  - 执行 `meta-kim-memory-save.mjs --event stop`

### 3. 项目级 `.codex/hooks/` 中的主要脚本

项目内 Codex hook 目录包含以下主要脚本：

- `graphify-context.mjs`
- `meta-kim-memory-save.mjs`
- `activate-meta-theory-spine.mjs`
- `utils.mjs`
- `spine-state.mjs`
- `skip-reminder.mjs`
- `hook-i18n.mjs`
- `hookprompt-adapter.mjs`

### 4. 用户级全局写入

本次安装在用户目录下为 Codex 明确写入了以下内容：

- `C:/Users/zhouy/.codex/skills/meta-theory/`

根据本次检查结果，可以明确确认：

- Codex 的全局 `meta-theory` 技能包已安装
- 当前未发现本次安装明确改写用户级 `C:/Users/zhouy/.codex/config.toml` 的直接证据
- 当前未发现本次安装明确改写用户级 `C:/Users/zhouy/.codex/hooks.json` 的直接证据

因此，本次对 Codex 的核心生效方式主要是：

- 项目内 `.codex/hooks.json`
- 项目内 `.codex/hooks/`
- 项目内 `.codex/agents/`
- 项目内 `.codex/skills/meta-theory/`
- 用户级 `C:/Users/zhouy/.codex/skills/meta-theory/`

## 五、安装状态记录文件

本次安装还写入了以下状态记录文件：

- `.meta-kim/local.overrides.json`
- `.meta-kim/install-manifest.json`

其中：

- `.meta-kim/local.overrides.json`
  - 记录当前启用目标为 `claude` 与 `codex`
- `.meta-kim/install-manifest.json`
  - 记录本次安装与同步实际落地的文件清单
  - 用于追踪项目级安装结果

## 六、结论

可以将本次安装结果概括为：

1. `Claude Code` 同时获得了项目级配置和用户级全局配置
2. `Codex` 获得了完整的项目级配置，并安装了用户级全局技能包
3. 当前仓库已经完成运行时投影、graphify 重建与项目完整性校验
4. 安装后的项目验证已通过

如果后续需要进一步审计，可继续做两类工作：

1. 对比 `Claude Code` 与 `Codex` 的 hook 行为差异
2. 对用户级全局配置做“新增项”和“合并项”的逐条审计
