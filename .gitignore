# ========== Dependencies ==========
node_modules/
npm-debug.log*
pnpm-lock.yaml
package-lock.json

# ========== Secrets (never commit; preventive — project has no .env yet) ==========
.env
.env.*
!.env.example

# ========== Local-only Claude skills (not synced via sync:runtimes; optional personal overlay) ==========
canonical/skills/commit-review/

# ========== Runtime Projections (Generated from canonical/) ==========
# These are DERIVED assets, NOT source. Edit canonical/ instead.
/.vscode/
/.claude/
/.mcp.json
/.codex/
/codex/
/.agents/
/openclaw/
/openclaw/workspaces/*/.openclaw/
/shared-skills/
/.cursor/

# ========== Local State (Never commit) ==========
/.planning/
.meta-kim/
.meta-kim/state/
.meta-kim/local.overrides.json
test-output/
# Browser-automation session debris. Untracked debris would otherwise join the
# graphify repository file set and bind the graph identity proof to scratch files.
/.playwright-mcp/

# ========== Code Knowledge Graph (Generated by graphify) ==========
graphify-out/
.meta-kim-graphify-previous-*/

# ========== Runtime Directories ==========
runtimes/
# Runtime source adapters are durable source, not generated runtime homes.
!/src/runtimes/
!/src/runtimes/**

# ========== Auto-generated Records (Not source code) ==========
memory/
.reports/

# ========== Operational Snapshots (Local Only) ==========
# These change frequently and are project-internal, not user-facing.
todos.json
docs/todo-list.md
docs/agent-health-report.md
scripts/generate-todo-list.mjs

# ========== Release Artifacts (Embedded in gh release bodies, not tracked) ==========
# npm pack output is a Release attachment, not source; retain local archives.
/meta-kim-*.tgz
# Release notes are published as gh release bodies; local drafts stay outside git.
.release-notes-v*.md
# Progress snapshots are historical, kept outside git.
progress-v*.md
# Versioned RFC drafts are internal-only, not public.
docs/v*-rfc-*.md

# ========== Test run output (generated; keep tests/*.test.mjs & fixtures tracked) ==========
tests/output/
tests/.cache/
tests/CC-e2e/

# ========== Planning & Progress (local scratch) ==========
task_plan.md
progress.md
findings.md

# ========== Backup / Temporary ==========
.backup/
tmp/

# ========== Terminal / Shell Cache Files ==========
*.omp.cache
pwsh.*.omp.cache
pip/

# Running the canonical Python memory hooks emits bytecode caches next to the
# source. Leaving them untracked would bind the graph identity proof to build
# debris that any later test run recreates or removes.
__pycache__/
*.py[cod]

# Windows CEF applications can inherit the repository cwd and emit this
# machine-local log while release verification is running.
/debug.log

# A PowerShell 7 spawn that inherits the repository cwd can drop its
# machine-local module-analysis cache here. The producing call site is not
# identified; the path is ignored on the observed artifact, not on a known
# mechanism. Leaving it untracked would bind the graph identity proof to
# the same scratch files.
/Microsoft/

# ========== Root-level images ==========
/images/

# ========== Lib directories ==========
# (docs/lib/ may be created by some build tools)
lib/
*/lib/
*/node_modules/
# Canonical shared libraries are source assets, not dependency output.
!canonical/runtime-assets/shared/lib/
!canonical/runtime-assets/shared/lib/**

# ========== Docs Privacy Boundary ==========
# docs/ is local-private by default. Keep only README images public.
docs/**
!docs/
!docs/live-sdk.md
!docs/images/
!docs/images/**
!docs/runtime/
!docs/runtime/**

# ========== Generated reports ==========
docs/agent-design-quality-eval-report.zh-CN.md
docs/capability-gap-codex-real-test-report.zh-CN.md
docs/capability-gap-isolated-task-report.zh-CN.md
docs/governance-agent-benchmark-review.zh-CN.md
docs/governance-agent-process-mvp-report.zh-CN.md
